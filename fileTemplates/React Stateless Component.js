import React from 'react';
import PropTypes from 'prop-types';

const ${NAME} = (props) => {
  return (
    <div>
      ...
    </div>
  );
};

${NAME}.propTypes = {
  //someProp: PropTypes.object.isRequired,
};

export default ${NAME};